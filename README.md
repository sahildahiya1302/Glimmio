# Glimmio Repository

The project source has been moved under `Glimmio-IMX/` to separate the platform code from other website content. The `public` directory within `Glimmio-IMX` contains all web assets and PHP endpoints.

Refer to `Glimmio-IMX/README.md` for details.
